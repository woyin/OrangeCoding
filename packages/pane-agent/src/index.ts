/**
 * @orangecoding/pane-agent - Terminal pane agent executable.
 *
 * This package is primarily a binary entry point (orangecoding-pane-agent).
 * The index re-exports nothing since this is a standalone CLI tool.
 */

export {};
