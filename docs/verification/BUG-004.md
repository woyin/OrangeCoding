# BUG-004 验证报告：memory recall 按 modified_at 排序

Result: **PASS**

## 修复是否完整
✅ 完整。`recall_memories` 在构建 `filtered_summaries` 后、调用 `selector.select()` 前，按 `modified_at` 降序排序。使用 enumerate + `sort_by` with `then_with(|| index)` 保证相同时间的条目保持原始顺序。

## 是否存在遗漏路径
✅ 无遗漏。`recall_memories` 是唯一的记忆召回入口，已修改。

## 是否影响性能或顺序语义
- 排序是 O(n log n)，候选列表通常 <100 条，性能影响可忽略
- 相同 `modified_at` 的记忆通过原始索引保持稳定顺序
- `build_manifest` 函数不受影响（它接收外部 candidates）

## 测试覆盖
- `test_recall_sorts_by_modified_at_newest_first` — unsorted 输入排序后新记忆优先
- `test_recall_sorting_with_max_results` — 排序 + MAX_RESULTS 截断不丢失最新记忆
- `test_recall_already_sorted_remains_correct` — 已排序输入不受影响
- `test_mock_selector_correct_return` — 更新为验证排序后的正确顺序
- 全部 16 个 memory_recall 测试通过
- 全量测试 0 失败

## 建议
- 排序逻辑使用 `enumerate` + `then_with` 做稳定排序，如果 Rust 未来版本改变 `sort_by` 的稳定性，当前实现仍然正确
